# 对拍

## 如何使用

- 将`ac.cpp`修改为'正确'输入输出的程序
- 将`wa.cpp`修改为你想测试的程序
- 将`data.cpp`修改为产生随机数据的程序
- 接下来只需要运行"`. run.sh`"（当发现`ac`和`wa`输出不同时会终止测试，否则将会一直运行)
- 当发现输出不同后，你可以查看`log/data.output`来获得输入样例，`log/ac.output`获得'正确'输出，'log/wa.output'获得'错误'输出